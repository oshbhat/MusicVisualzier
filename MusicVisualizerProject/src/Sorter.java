
//Here is where the sorting happens for a song
//This class handels the sorting for each characterization of a song, i.e name, year, etc
public class Sorter extends LinkedList<Song> {

    private Node<Song> curr;
    private Node<Song> head;
    private Node<Song> prev;


    /*
     * have different sorting methods
     *
     * sortByArtist
     * sortByGenre
     * sortByName
     * sortByName
     *
     * Uses a link list and nodes in order to determine how to sort each of these categories for a song
     *
     *
     */
}